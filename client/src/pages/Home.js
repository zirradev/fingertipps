import React from "react";
import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <Box>
      <Typography variant="h1">Hello FigerTips</Typography>
    </Box>
  );
};

export default Home;
